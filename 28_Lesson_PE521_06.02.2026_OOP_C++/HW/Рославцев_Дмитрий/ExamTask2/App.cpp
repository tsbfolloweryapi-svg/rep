#include "App.h"

/*
// вывод вектора на экран
void App::showAsTable(const string& title)
{
	cout << title;

	auto data = vectorString_.getData();
	for_each(data.begin(), data.end(), [](float item) {
		cout << setw(10) << item;
	});
} // App::showAsTable

// 1)	—формируйте частотный словарь слов текста
*/
